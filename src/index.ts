/** @format */

import { DatabaseServer } from "./server/databaseServer.js";
import { DatabaseManager } from "./client/databaseManager.js";

export { DatabaseManager, DatabaseServer };
export default { DatabaseManager, DatabaseServer };
